import SwiftUI

struct SettingsView: View {
    var body: some View {
        Text("Settings — Connection tab arrives in C2 Task 3")
            .padding()
            .frame(width: 460, height: 300)
    }
}
