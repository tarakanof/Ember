/// Build/scaffold marker. Real types replace this in later tasks; the enum is a
/// namespace so the test can confirm the module links without a stray global.
public enum AwtrixMenuKit {
    public static let marker = "awtrix-menu-kit"
}
